---
$schema: https://json-schema.org/draft/2020-12/schema
$id: https://www.ammitto.org/schemas/ch-announcement.yml
title: Switzerland Sanction Announcement
description: Schema for Swiss SECO sanction announcements
type: object
required:
  - id
  - announcement
  - sanction_details
properties:
  id:
    type: string
    description: Unique identifier for this announcement
    pattern: "^ch/[a-z0-9-]+$"

  announcement:
    type: object
    required:
      - title
      - publish_date
    properties:
      title:
        type: string
        description: Title of the announcement
      url:
        type: string
        format: uri
        description: URL to official source
      publish_date:
        type: string
        format: date
        description: Publication date (YYYY-MM-DD)
      document_id:
        type: string
        description: Official document identifier
      document_type:
        type: string
        description: Type of document (links to document-types.yml)
      authority:
        type: string
        description: Issuing authority (links to organizations.yml)
      publisher:
        type: string
        description: Publisher (links to organizations.yml)
      lang:
        type: string
        pattern: "^[a-z]{2,3}(-[A-Za-z]{4})?(-[A-Z]{2})?$"
        description: Language code (e.g., "de", "fr", "it")
        default: de
      content:
        type: string
        description: Full announcement text

  sanction_details:
    type: array
    items:
      type: object
      required:
        - entity
        - measures
      properties:
        entity:
          type: object
          required:
            - type
            - name
          properties:
            type:
              type: string
              enum: [person, organization, vessel, aircraft]
            name:
              type: object
              properties:
                full_name:
                  type: string
                first_name:
                  type: string
                last_name:
                  type: string
                script:
                  type: string
                  description: ISO 15924 script code
            aliases:
              type: array
              items:
                type: string
            identifiers:
              type: array
              items:
                type: object
                properties:
                  type:
                    type: string
                  value:
                    type: string
                  issuing_country:
                    type: string
            addresses:
              type: array
              items:
                type: object
                properties:
                  street:
                    type: string
                  city:
                    type: string
                  country:
                    type: string
                  country_code:
                    type: string
                  postal_code:
                    type: string
                  care_of:
                    type: string
            birth_info:
              type: object
              properties:
                year:
                  type: string
                date:
                  type: string
                  format: date
                place:
                  type: string
                country:
                  type: string
            nationalities:
              type: array
              items:
                type: string
            remarks:
              type: string

        measures:
          type: array
          items:
            type: object
            properties:
              type:
                type: string
                enum:
                  - asset_freeze
                  - travel_ban
                  - arms_embargo
                  - trade_restriction
                  - other
              description:
                type: object
                description: Localized description
        reasons:
          type: array
          items:
            type: object
            properties:
              category:
                type: string
              description:
                type: object
        period:
          type: object
          properties:
            effective_date:
              type: string
              format: date
            expiry_date:
              type: string
              format: date
            is_indefinite:
              type: boolean

  legal_citations:
    type: array
    description: Legal instruments cited as basis
    items:
      type: object
      properties:
        legal_instrument_id:
          type: string
          description: Reference to legal instrument
        articles:
          type: array
          items:
            type: string
        citation_type:
          type: string
          enum: [legal_basis, reference, amendment]

  group_id:
    type: string
    description: ID for grouping multiple entries
